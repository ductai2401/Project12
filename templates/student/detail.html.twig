{% extends 'base.html.twig' %}

{% block body %}
	<div class="container col-md-6 mt-3">
		<h3 class="text-primary text-center text-capitalize" style="font-family:Papyrus;">STUDENT DETAIL</h3>
		<a class="btn btn-outline-dark" href="{{ path('student_index') }}">STUDENT LIST</a>
		<table class="table text-center mt-3", style="background-color: lightblue">
			<thead style="background-color: black">
				<tr style="color: #FFFFF0">
					<th>Name</th>
					<th>Avatar</th>
					<th>Age</th>
					<th>Address</th>
					<th>Phone</th>
					<th>Course</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ student.name }}</td>
						<td>
								<img src="{{ asset('images/student/'~student.avatar )}}" alt="avatar" width="100" height="120">
						</td>
                        <td>{{student.age}}</td>
                        <td>{{student.address}}</td>
                        <td>{{student.phone}}</td>
					<td>
						{% for b in student.course %}
							<a href="{{ path('course_detail', {'id': b.id }) }}">
								{{ b.name }}
							</a>
							<br>
						{% endfor %}
					</td>
					{% if is_granted('ROLE_ADMIN') %}
						<td>
							<a class="btn btn-info" href="{{ path('student_edit', {'id': student.id}) }}">EDIT</a>
							<a class="btn btn-warning" href="{{ path('student_delete', {'id': student.id}) }}"
							onclick= "return confirm('Are you sure to delete this student ?')"
							>DELETE</a>
						</td>
					{% endif %}
				</tr>
			</tbody>
		</table>
	</div>
{% endblock %}
