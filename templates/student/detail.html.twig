{% extends 'base.html.twig' %}

{% block body %}
	<div class="container col-md-6 mt-3">
		<h3 class="text-success text-center text-capitalize">STUDENT DETAIL</h3>
		<a class="btn btn-outline-success mt-2" href="{{ path('student_index') }}">STUDENT LIST</a>
		<table class="table table-striped text-center mt-2">
			<thead>
				<tr>
					<th>Name</th>
					<th>Avatar</th>
					<th>Age</th>
					<th>Address</th>
					<th>Phone</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ student.name }}</td>
						<td>
								<img src="{{ asset('student/'~student.avatar )}}" alt="avatar" width="100" height="120">
						</td>
                        <td>{{student.age}}</td>
                        <td>{{student.address}}</td>
                        <td>{{student.phone}}</td>
					{# <td>
						{% for b in teacher.courseList %}
							<a href="{{ path(course_detail', {'id': b.id }) }}">
								{{ b.name }}
							</a>
							<br>
						{% endfor %}
					</td>
					<td>
                  {% for m in teacher.magazines %}
							<a href="{{ path('magazine_detail', {'id': m.id }) }}">
								{{ m.title }}
							</a>
							<br>
						{% endfor %}
					</td> #}
					<td>
						<a class="btn btn-info" href="{{ path('student_edit', {'id': student.id}) }}">EDIT</a>
						<a class="btn btn-warning" href="{{ path('student_delete', {'id': student.id}) }}"
                  onclick= "return confirm('Are you sure to delete this student ?')"
                  >DELETE</a>
					</td>
				</tr>
			</tbody>
		</table>
	</div>
{% endblock %}
